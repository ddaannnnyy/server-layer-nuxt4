{
  "name": "server-best-practice",
  "type": "module",
  "version": "1.0.1",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "release:latest": "npx changelogen@latest --release && npm publish && git push --follow-tags",
    "release:bump": "npx changelogen@latest --bump && npm publish && git push --follow-tags"
  },
  "peerDependencies": {
    "zod": "^4.3.6"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "vue": "^3.5.32",
    "vue-router": "^5.0.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^8.2.0",
    "eslint": "^10.2.0",
    "eslint-plugin-format": "^2.0.1"
  }
}
